server {
	listen 80;

	location /api1/ping {
		proxy_pass http://api1:5001/ping;
	}

	location /api2/ping {
		proxy_pass http://api2:5002/ping;
		proxy_http_version 1.1;
		proxy_set_header Upgrade $http_upgrade;
		proxy_set_header Connection "upgrade";
	}
}
